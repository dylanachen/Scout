VITE_API_URL=http://localhost:8000
VITE_WS_URL=ws://localhost:8000

# No backend? Use mock REST + local-only chat (restart dev server after changing).
# VITE_DEMO_MODE=true
# Accepted: true, 1, yes (any casing)
